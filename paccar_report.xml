

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566745600]" time="0.052"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566745600'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566748928]" time="0.065"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566748928'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566749184]" time="0.052"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566749184'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566753280]" time="0.064"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566753280'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566755587]" time="0.066"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566755587'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566757120]" time="0.064"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566757120'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566758144]" time="0.056"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566758144'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566763008]" time="0.061"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566763008'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566763264]" time="0.062"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566763264'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566763520]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566763520'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566764544]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566764544'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566767872]" time="0.065"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566767872'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566767875]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566767875'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566770688]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566770688'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566776064]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566776064'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566801923]" time="0.065"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566801923'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566802179]" time="0.062"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566802179'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566804992]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566804992'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566809856]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566809856'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566811651]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566811651'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566822400]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566822400'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566824960]" time="0.060"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566824960'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566831360]" time="0.060"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566831360'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566832128]" time="0.051"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566832128'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566832384]" time="0.061"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566832384'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566834688]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566834688'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566834691]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566834691'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566834944]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566834944'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566834947]" time="0.061"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566834947'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566835459]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566835459'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566835712]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566835712'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566838784]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566838784'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566838787]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566838787'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566839040]" time="0.060"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566839040'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566839296]" time="0.056"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566839296'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566839552]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566839552'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566840064]" time="0.053"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566840064'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566840320]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566840320'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566840323]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566840323'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566840576]" time="0.056"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566840576'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566840591]" time="0.065"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566840591'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566840835]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566840835'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566841088]" time="0.063"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566841088'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566841344]" time="0.052"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566841344'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566841600]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566841600'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566842112]" time="0.064"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566842112'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566842624]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566842624'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566843136]" time="0.064"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566843136'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566843139]" time="0.052"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566843139'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566843392]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566843392'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566843395]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566843395'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566843648]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566843648'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566843904]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566843904'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566844160]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566844160'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566844672]" time="0.063"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566844672'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566844928]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566844928'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566845696]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566845696'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566845952]" time="0.055"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566845952'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566846208]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566846208'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566846467]" time="0.057"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566846467'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566847488]" time="0.066"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566847488'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566847744]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566847744'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566848256]" time="0.056"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566848256'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566849280]" time="0.054"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566849280'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566867456]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566867456'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2566889475]" time="0.056"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2566889475'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2631139072]" time="0.054"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2631139072'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2633750272]" time="0.060"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2633750272'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2633844483]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2633844483'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2633929216]" time="0.066"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2633929216'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2633937152]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2633937152'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2633937664]" time="0.059"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2633937664'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2633941504]" time="0.058"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2633941504'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase><testcase classname="unit_test_v0" name="test_paccar_routing_logic[GATEWAY: VCAN20:VCAN2 | ID: 2633941519]" time="0.062"><failure message="Failed: [FAIL] Gateway dropped the frame (Timeout).">active_buses = {'ADSCAN1': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166364F1BF0&gt;, 'ADSCAN2': &lt;can.interfaces.virtual.Virtu...rtualBus object at 0x00000166366C8550&gt;, 'PCAN2': &lt;can.interfaces.virtual.VirtualBus object at 0x00000166366C0C30&gt;, ...}
senderName = 'VCAN20', receiverName = 'VCAN2', arbitrationID_raw = '2633941519'

    @pytest.mark.parametrize("senderName, receiverName, arbitrationID_raw", test_cases, ids=test_names)
    def test_paccar_routing_logic(active_buses, senderName, receiverName, arbitrationID_raw):
    
        # --- 3. THE TOPOLOGY EXECUTION LOOP ---
    
        # Point our sender and receiver variables to the already-open buses
        sender = active_buses[senderName]
        receiver = active_buses[receiverName]
    
        is_sender_fd = senderName in CAN_FD
        is_receiver_fd = receiverName in CAN_FD
    
        # CAN Signal Injection Logic
        int_arbitrationID = int(utils.format_arbitrationID(arbitrationID_raw, "int"))
    
        # randomized data payload everytime a new arbitration id is put on the bus
        dummy_data = [random.randint(0, 255) for _ in range(8)]
    
        # FORMAT SENDER (What goes ONTO the bus)
        msg = None
    
        if is_sender_fd:
            send_id, send_payload = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            msg = can.Message(is_rx=False, is_extended_id=True, is_fd=True, bitrate_switch=True, dlc=9,
                              arbitration_id=send_id, data=send_payload)
        else:
            # I accidentally deleted this...
            msg = can.Message(is_rx=False, is_extended_id=True,
                              arbitration_id=int_arbitrationID, data=dummy_data)
    
        # if msg isn't formatted correctly according to is_sender_fd boolean, then I want to throw an error so I know that the boolean is the issue
        if msg is None:
            pytest.fail(f"FATAL SCRIPT LOGIC: Formatting bypassed for ID {arbitrationID_raw}. Halting to prevent ghost frame injection.")
    
        # FORMAT RECEIVER (What comes OFF the bus)
        if is_receiver_fd:
            expected_id, expected_data = utils.generate_j1939_22_envelope(int_arbitrationID, dummy_data)
            expected_is_fd = True
        else:
            expected_id = int_arbitrationID
            expected_data = dummy_data
            expected_is_fd = False
    
        # --- 5. EXECUTION &amp; RETRY LOGIC ---
        MAX_RETRIES = 1 # Set to 1 for development, but change to 5 for final testing and reporting
        found_routed_frame = False
        receivedMessage = None
    
        # want to check how long it takes for a pass to test
        elapsed_time_ms = 0.0
    
        for attempt in range(MAX_RETRIES):
            formatted_send_payload = " ".join(f"{x:02x}" for x in msg.data)
            print(f"\n -&gt; Sending to {senderName}  : 0x{msg.arbitration_id:08X} | {formatted_send_payload} (Attempt {attempt + 1})")
    
            # flush receiver at the start of each gateway test attempt by
            # checking the receiver for CAN messages, until there's nothing being received
            # THEN we start inject the new CAN message
            while receiver.recv(0.0) is not None:
                pass
    
            # Get Current time
            start_time = time.time()
    
            sender.send(msg)
    
            # Find current time then add 1.0 seconds to it
            # change 1.0 to 0.05 for faster timeout during development, but make sure to change it back to 1.0 for final testing and reporting since some frames can take a while to route through the gateway
            timeout_end = start_time + 0.05
    
            # watchdog timer for checking receiving bus for however long the difference is between time() and timeout_end
            while time.time() &lt; timeout_end:
                # change timeout value in recv() to 0.01 for faster checking during development, but make sure to change it back to 0.1 for final testing and reporting since some frames can take a while to route through the gateway
                receivedMessage = receiver.recv(0.01)
    
                # With randomized data, we can now check which ECU CAN port actually received the payload
                if receivedMessage and list(receivedMessage.data) == expected_data:
                    elapsed_time_ms = (time.time() - start_time) * 1000
                    found_routed_frame = True
                    break
    
            if found_routed_frame:
                break
    
        # --- EVALUATION ---
    
        if not found_routed_frame:
&gt;           pytest.fail(f"[FAIL] Gateway dropped the frame (Timeout).")
E           Failed: [FAIL] Gateway dropped the frame (Timeout).

unit_test_v0.py:225: Failed</failure></testcase></testsuite></testsuites>